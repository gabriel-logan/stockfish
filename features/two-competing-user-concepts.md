# Two Competing User Concepts

- [x] Resolved

The project has `authStore` for the API-authenticated user and `userStore` for local users.

Both appear together in the layout, and some flows, such as saving an online game, mix those responsibilities.
